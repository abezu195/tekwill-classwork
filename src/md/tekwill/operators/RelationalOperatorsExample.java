package md.tekwill.operators;

public class RelationalOperatorsExample {
    public static void main(String[] args) {
        boolean myBoolean = true;

        long myLong = 10L;

//        myBoolean >= myLong; // We can't compare variables of different data types

//        myBoolean >= true; // We can't compare boolean values, since it does not make sense in real life too
    }
}
